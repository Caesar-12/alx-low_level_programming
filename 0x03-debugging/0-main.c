#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description: Check if func runs properly
 * Return: 0 if successful
 */

int main(void)
{
	int i;

	i = 25;
	positive_or_negative(i);

	return (0);
}
