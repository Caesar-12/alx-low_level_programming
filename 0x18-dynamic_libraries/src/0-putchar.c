#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Description: C Functions
 * Return: 0 when successful
 */

/*int main(void)
{
	int i = 0;
	char value[] = "_putchar";

	while (value[i] != '\0')
	{
		putchar(value[i]);
		i++;
	}

	putchar('\n');
	return (0);
}*/
